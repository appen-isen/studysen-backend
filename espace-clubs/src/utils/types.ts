export type Club = {
  clubId: number;
  name: string;
  campusId: number;
  imageUrl: string;
};

export type Post = {
  id: number;
  title: string;
  content: string;
  createdAt: string;
  // Ajoute d'autres champs si besoin
};
